# Bionic Reading — TODO

## Shipped (v2)

- [x] Content engine rewrite — whitespace/HTML-preserving, DOM-node injection (never `innerHTML`), no re-processing of own output, single declarative injection, faithful restore, smart denylist scope, best-effort shadow DOM, chunked processing.
- [x] Controls via `chrome.storage` — master toggle, Intensity (Light/Medium/Strong), Highlight prefix (bold always on), Auto-apply on every site, per-site memory by hostname.
- [x] New brand + "fixation lines" icons, generated by `python3 icons/build_icons.py`.
- [x] Keyboard shortcut `Ctrl+Shift+E` / `Command+Shift+E`.
- [x] Docs refreshed — README, INSTALLATION, TESTING.
- [x] Packaging script (`package.js`) produces a clean runtime-only zip.

## Ideas / backlog

- [ ] Dedicated options page for richer settings.
- [ ] Option to dim (rather than just not-bold) the non-prefix part of each word.
- [ ] Per-site blocklist UI (manage remembered sites; "never run here").
- [ ] Firefox port (Manifest V3 / WebExtensions).
